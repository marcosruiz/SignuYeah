<resources>

    <style name="MyButtonStyle" parent="Widget.AppCompat.Button.Colored">
        <item name="android:textColor">@color/textColor</item>
        <item name="android:textColorPrimary">@color/textColor</item>
    </style>

    <style name="MyEditTextStyle" parent="Widget.AppCompat.EditText">
        <item name="android:textColor">@color/textColor</item>
    </style>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="android:windowBackground">@color/colorBackground</item>
        <item name="android:colorBackground">@color/colorPrimary</item>
        <item name="android:panelFullBackground">@color/colorBackground</item>
        <item name="android:panelBackground">@color/colorBackground</item>
        <item name="android:panelColorBackground">@color/colorBackground</item>
        <item name="android:popupBackground">@color/colorBackground</item>
        <!--<item name="android:colorBackgroundFloating">@color/colorBackground</item>-->
        <!--<item name="android:windowBackground">@color/colorBackground</item>-->
        <item name="android:actionMenuTextColor">@color/colorBackground</item>


        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccentDark</item>
        <item name="colorButtonNormal">@color/colorAccent</item>
        <item name="editTextColor">@color/textColor</item>

        <item name="android:textColorPrimaryDisableOnly">@color/textColorPrimaryDisableOnly</item>
        <item name="android:textColorHint">@color/textColor</item>
        <item name="android:windowActionBar">@color/colorAccentDark</item>
        <item name="android:textColor">@color/textColor</item>
        <item name="colorControlNormal">@color/textColor</item>


        <!-- API 23 -->
        <!--<item name="android:titleTextColor">@color/colorAccent</item>-->
        <!--<item name="android:subtitleTextColor">@color/textColor</item>-->

        <!-- styles -->
        <item name="buttonStyle">@style/MyButtonStyle</item>
        <item name="editTextStyle">@style/MyEditTextStyle</item>

    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
